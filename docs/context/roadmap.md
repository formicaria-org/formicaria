# Roadmap — folded into `plan.md`

This file's content — the near-term single-user work (calendar ICS import/export,
whiteboard-in-a-note + print-to-PDF, the `Source` module, the append-only blob mirror) —
is now **Track S** of the single sequenced program in [`plan.md`](./plan.md), alongside
the collaboration track. There is one forward document now, not two.

> **Go to [`plan.md`](./plan.md).** Deep-dive receipts for the collaboration track live in
> [`collaboration-design.md`](./collaboration-design.md).

_(Kept as a stub so older links — including session-log entries that cite `roadmap.md:NN`
line numbers — still resolve. Those line references point at this file's pre-fold history;
git has it.)_
