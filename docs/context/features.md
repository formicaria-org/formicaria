# Features — the index (always-read)

One line per feature: **what it is · status · where the detail lives.** This is the discovery layer —
it names what exists and points at the on-demand doc, so nothing durable is invisible. Keep it
**brief**: a feature earns a dedicated `features/<slug>/` doc only when its lessons overflow a line
here. Status: ✅ shipped · 🟚 partial · 📋 planned. "the code wins" — verify a claim before trusting it.

| Feature | Status | One line | Detail / plan |
|---|---|---|---|
| **Notes & storage** | ✅ | files-as-truth: one note = one `<ulid>.md`; `Store`/`FileStore`/`MultiStore`, FTS5 index (disposable) | `overview.md` (seams) · `decisions.md#seams` |
| **Query & views** | ✅ | pure `fm-query` engine; board/agenda/timeline/search, generic literal-free renderers — **the feed is a place you read and discuss since 2026-08-31**: a post carries a char-capped
`excerpt` (several lines, markdown stripped, filled only by `recent()`) faded out with a mask, is an
`<article>` whose title+picture is the one button that opens the note, and carries a reply count
from a single `thread_roots` call — tap it and one discussion expands with the last three messages
and a box, flat and reply-only. Editing stays in the pane (one editor per note), bodies are never
Markdown-rendered in a list, and there is no double-tap; **the timeline has two densities since
2026-08-30**: a *feed* (default) showing each note as a post with its picture, its first line, its vault and its last editor, which is what makes a shared vault legible as one stream; and the original one-line *list*, switched per pane like the agenda's month/week/list. The feed mounts 30 posts with a button for the rest, because `recent` is unbounded and nothing here is virtualised, `.view` files — **a filtered view names what it leaves out and offers the unfiltered one; the board carries a column rail** (2026-08-24); **saveable from the app** — an arrangement you keep, not a query you write (2026-08-28), **deletable and renameable, recorded in git** (2026-08-30). **The whole authoring surface was withdrawn on 2026-08-31** — no save, rename, delete or board group-by anywhere in the UI, because it was reachable before anyone could learn what a view is; the commands and their tests stay, and a `.view` file written by hand or arriving over git still lists and opens, so the promised design plan is cheap to land. **The board's column rail went with it**, against advice: a filtered view still says what it leaves out, but a board that scrolls no longer says so | `overview.md` · `decisions.md#ui` |
| **Appearance / themes** | ✅ | `<vault>/themes/*.css` — **git-tracked, so a theme travels with the notes; which one is on stays per-device** (like dark/light). Two ways in over one file: a three-question form (accent, text size, font) and a CSS box with live preview. A file richer than the form is shown read-only and **refused, never flattened**. The escape hatch is an **armed-boot guard** (JS, not CSS): a theme nobody could interact with is not put back. `SUPPORTED` in `appearance.ts` is the promised surface — CI fails if the form names a token itself | `decisions.md#ui` (*a theme is a file in the vault*) |
| **Editor & workspace** | ✅ | pane-grid workspace, `/` slash menu at caret, note↔note refs, double-click-to-edit. **Chrome is one element in two placements (2026-08-30)**: a collapsible panel down the left where there is width, the same controls as a bottom bar where there is not — reverses the 2026-08-28 rail removal, because this one *replaces* the top bar instead of joining it. **One view at a time is the default since 2026-08-31, and `auto` is gone**: opening a view *re-points* a window that already shows it rather than stacking another (`focusOrOpen`), the open-windows strip is desktop-only, and the arrangement is a set of inherited custom properties so a narrow rule can no longer be half-written — `tiled` stays, opt-in. **The chrome is one row at the top since 2026-08-31**: the pane header is gone in `single` (it belongs to `tiled`, where names, drag handles and per-pane controls earn their row) and `ViewControls.svelte` renders the active view's own controls — grouping, density, a search pane's query box, a saved view's rename/delete — at the end of the bar that names it. Save view moved to the action list; the contributor filter was removed outright; the vault filter is a `menuitemcheckbox` menu that states how much it hides. **Every dialog is bounded by the visible viewport since 2026-09-01** — capped in `dvh` inside the window insets, one scroll surface on the panel, after Back up shipped with no cap and no `overflow` at all (its own primary button fell off the bottom) and Help shipped with no overlay CSS at all, because Svelte scopes `.sheet` to the component that declares it | `decisions.md#ui` (*the chrome moves to a collapsible side panel*; *an overlay is bounded by the visible viewport*) |
| **Media / blobs** | 🟚 | content-addressed blobs, ingest, streaming `GET /api/blob`; **video capped**; phone byte path is base64-over-IPC. **PDFs render on the desktop since 2026-08-29** — `frame-src 'none'` had been blocking the read view's own iframe there, silently, since the clause was written. **On Android they never have and still do not**: the WebView cannot render a PDF inline and the phone serves every blob as `octet-stream`, so `open_external` remains the only way (`decisions.md#track-m`) | `known-issues.md` · `decisions.md#vault` · `decisions.md#ui` |
| **Whiteboard** | 🟚 | embedded Excalidraw, element-wise scene merge; **canvas-in-embed** still open, unverified in headless | `decisions.md#ui` · `known-issues.md` |
| **Collaboration (git sync)** | ✅ | per-vault git, `.md` merge driver, pull/push/squash, proposals (create/review/accept/reject), cross-user; **conflicts resolvable in-app for every git kind** (keep a side / mark resolved). **The phone can merge prose since 2026-09-02** — `text_3way` shelled out to `git merge-file` unconditionally, so any pull that had to merge a diverged body died mid-merge on Android and left a `MERGE_HEAD` that froze the vault; libgit2's `git_merge_file` is now the engine where there is no binary, held byte-identical to real git over 400 generated triples. The agent's own commit authorship survives there too (`commit_all_as` had no libgit2 arm) | `decisions.md#git` · `decisions.md#sync` · `sessions/2026-07-24-proposals-on-the-phone.md` |
| **Share with a nearby device** | 🟚 | pair a tablet over the LAN; **per-vault scope**, code→`HttpOnly` token, host-bound commands denied; TLS (self-signed leaf, fingerprint checked by hand) unlocks the mic | `decisions.md#vault` (Scope) · `decisions.md#toolchain` (TLS) · `known-issues.md` |
| **Mobile / Android** | 🟚 | one shared Rust core on the phone, git-coordinated via libgit2 (`native-git`); jniLibs + foreground service; startup is a contract (`pixi run android-smoke`). **Responsiveness fixed 2026-08-20** — IPC commands are `(async)` (a blocking one parks the JS thread), no per-autosave full-corpus scans, incremental cold start, 16 MB ingest ceiling; **the suite now runs the phone's own branches** (`asPhone()` in `ui/src/lib/harness.ts`). **The shell reads its own window insets since 2026-08-31** — `MainActivity` forwards `WindowInsetsCompat` into the page as the `--safe-*` tokens, closing the guessed floors that put the top bar under the camera cutout and the bottom chips under the navigation bar; `app.css` keeps a floor as the pre-bridge fallback. **iOS boots and can sync** (rungs 2 and 4, 2026-09-03): on a Simulator the app builds, installs, launches and renders the real welcome screen — which the UI only draws once `list_vaults` and the status heartbeat have both answered, so `App::load()`, SQLite, the file store and libgit2 all work there — and **libgit2 reaches an HTTPS remote**, so Apple's SecureTransport and its trust store carry git where the Android CA workaround is `cfg`'d off. **Meant to reach users' phones since 2026-09-03, and the route is now chosen**: an **unsigned `.ipa`** each user signs with their **own free Apple ID** and installs over USB (SideStore/AltStore, or AltServer-Linux/`xtool` on Linux) — no App Store, no $99, no CI secret, no UDIDs, so the project never observes who runs it. `rung=5` (`ci/ios-package.sh`) builds it and asserts it is device-platform, single-arch, unsigned and free-team-signable. **It has never been installed on hardware — there is still no Mac and no iPhone here** — so nothing proves it re-signs, launches or syncs, and no user-manual page ships until someone with a phone says it does. Still agent-free by construction, assistant/audio deliberately off it. Rung 3 (phone-shaped UI) and rung 5 are built but undispatched | `mobile-design.md` · `ios-plan-2026-09-02.md` · `decisions.md#track-m` · `known-issues.md` (what is still open) |
| **Backup & acquisition** | ✅ | two-tier: git push (notes, plus attachments under the vault's `git_assets_max`, itself bounded by a **100MB ceiling** since 2026-09-02 — there is no git-lfs, so the app refuses to write a limit above it and clamps one it did not write) + restic (an opt-in snapshot of the notes dir **and** `blobs/` — never the vault root, so `views/`/`themes/`/history are git's alone); clone/restore via `acquire::naturalise`. **Works on Windows without installing git** since 2026-08-28 — libgit2 is linked where the OS ships no binary, and `vcs` still prefers a real one where there is. **Set up entirely in the app since 2026-08-29**: an access token for an HTTPS remote, a restic repo per vault, and one restic password per machine (`0600`, never in `vaults.json`) — and a *first* backup now records the notes instead of reporting success over an empty repo. **Every surface states its own tier's scope since 2026-09-02**, and the restic writers (`set_restic_repo`/`set_restic_password`/`clear_restic_password`) are refused to paired devices like `/api/backup` already was. **Surviving an update is a shipped step since 2026-09-02** — each release is its own folder with the vault inside it, so the archive carries `Update from an older folder.{sh,command,bat}`, which copies the old vault *in* (never `vaults.json`, which holds absolute paths) and never writes to or deletes the old folder | `decisions.md#git` (Backup, the Windows widening, the first-backup ordering) · `decisions.md#vault` (the restic password; what each tier carries, and why git-lfs is not taken) · `decisions.md#toolchain` (the update path) |
| **Papers / library** | 🟚 | a paper is a **note** tagged `paper` with flat metadata; created from a pasted BibTeX entry, DOI, arXiv link or bare title, and the identifier a PDF prints on itself is read at ingest — **all offline, the core links no HTTP client**. BibTeX back out. A `Papers` view is one saved tag filter. **No reader or annotations yet** (that is Phase C) | `decisions.md#data` (*A paper is a note*) · the papers plan |
| **Study assistant (agent)** | ✅ | on-device LLM answers `@name`, researches (opt-in web), drafts note edits you accept; one-hop RAG. **It installs itself from the download on Linux, macOS and Windows** (2026-09-02): the archive carries `agent-serve` + the catalogue, first enable asks which model and states its size and licence, and fetches runtime + weights with progress, cancel and resume into `<config>/formicaria/tools`. Support is decided by a **live memory reading**, not an OS list — a platform that cannot be read refuses with a reason. Android bundles the whole stack. **Never yet run on macOS or Windows**: those two are verified only as far as compiling and reading memory (`cross.yml`) | `decisions.md#agent` · `transcription-specialists-grounded-2026-08-31.md` |
| **Audio transcription** | 🟚 | `/transcribe` → whisper → proposal; multi-clip, skip-accepted. **Installs itself too since 2026-09-02**, as a second ~170 MB download behind its own switch, on **Linux and Windows**; **macOS has no upstream build** at the pinned whisper.cpp version and the row says so rather than offering a switch that would fail. The phone bundles it. Its capability is reported correctly on Android since 2026-09-02 — `agent_status` answered a shorter shape than the desktop's, so the shared panel showed "not available" where everything was in fact bundled (`decisions.md#track-m`, CI-guarded). **Confirmed on the owner's phone** (v0.3.1 / versionCode 3001) | `audio-asr-research-2026-07-23.md` · `sessions/2026-07-24-proposals-on-the-phone.md` |
| **Image → text (in `/transcribe`)** | 🟚 | handwriting, boards and photographed pages → a **digital transcript**: math as LaTeX, code as fenced blocks, figure labels kept. Same verb as audio, riding the same insertion-only adjunct substrate as `/transcribe` (`fm_agent::adjunct`). Runs on the **vision model already served** — the laptop pick has been `qwen3-vl-4b` since 2026-07-24 — enabled by fetching one `mmproj` projector beside the weights. **Desktop only**; the phone's model cannot see and says so rather than inventing a reading | `decisions.md#agent` (*a second modality specialist*) |
| **Import from Logseq / Obsidian** | ✅ | point at a graph and it becomes notes: `[[links]]` resolved to `note:<ULID>`, `#tags` hoisted into frontmatter, attachments into the blob store, journals dated by the day they name. **The source folder is only ever read.** A Logseq page keeps its outline as nested bullets — the atom stays the file, so `id::`/`((refs))` resolve to text rather than becoming block identity. **Add-only on a re-run** (`source_key`), because `refuse_if_stale` cannot see an edit made in-app and "update" would silently overwrite it. Preview first (`check_import`), one commit after, host-only | `decisions.md#data` (*An import converts; adoption renders*) |
| **Image → LaTeX** (a *dedicated* recogniser) | 📋 | a measured spike, behind an onnxruntime NOTICE gate; no defensible phone path yet. **Not the same thing as the row above**: the vision model already returns mathematics as LaTeX today (the user manual's study-assistant chapter), and this row is the specialised pipeline that would beat it on dense notation | `archive/ai-agents-plan-superseded-2026-09-02.md` (Part III) · `outstanding.md` |

**Forward work queue:** [outstanding.md](./outstanding.md). **Why anything is the way it is:**
[decisions.md](./decisions.md) (grep a `#subject`). **Open gaps & traps:** [known-issues.md](./known-issues.md).
