# The runtime binary + its shared libs, and the model weights, live here but never travel: they are
# large, machine-specific, and re-fetchable from models.toml. Only the manifest + scripts commit.
runtime/
models/
*.log
