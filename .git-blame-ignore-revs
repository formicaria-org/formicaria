# Revisions that changed formatting only. `git blame --ignore-revs-file .git-blame-ignore-revs`
# skips them; GitHub honours this file automatically.
#
# Keep this list to commits that are *purely* mechanical. A commit with one real change in it does
# not belong here — blame would step over that change too, which is worse than the noise.

# 2026-09-07 — adopt rustfmt (settings measured against the code; comments untouched)
0b51a28334f5201531939ff0a11a024eaa681276
